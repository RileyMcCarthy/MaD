# Documentation site toolchain (GitHub Pages build + local preview).
#   pip install -r docs/requirements.txt
#   mkdocs serve      # local preview at http://127.0.0.1:8000
#   mkdocs build      # static site → ./site
mkdocs-material==9.5.39
